{
  "captureDate": "2026-08-18",
  "portalPage": "https://publisher.unity.com/sales",
  "endpoint": "/publisher-v2-api/user",
  "method": "GET",
  "accountContext": "Signed-in publisher with a non-empty organization and country list.",
  "captureMethod": "A temporary opt-in local helper requested the same-origin endpoint and parsed the JSON in the Portal page. It replaced private values before output. It did not log or retain cookies, CSRF values, headers, email addresses, or raw response JSON.",
  "sanitization": [
    "Replaced user, publisher, and organization identifiers with stable numeric strings.",
    "Replaced person, organization, locale, country, and avatar values.",
    "Preserved key spelling, nesting, JSON types, nullability, and array/object containers."
  ],
  "boundarySemantics": "Not applicable."
}
